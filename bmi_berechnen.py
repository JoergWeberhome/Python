def berechne_bmi(weight, height):  # berechne den BMI
    print(weight)

    print(height)

    bmi = round(weight / (height * height), 2)
    # height = height/100
    # x = height * height
    # x = weight / x

    print(bmi)

    # bmi = round(x,2)


    # print(str(bmi))
    return bmi
